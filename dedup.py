# dedup.py — Stub for workout deduplication logic

def should_merge(xml_workout, fit_workout):
    """Stub — decide if a .fit and XML workout should be merged."""
    return False

def merge_fit_into_apple(xml_workout, fit_workout):
    """Stub — merge two workout records (XML preferred)."""
    return xml_workout